# Folio — Free PDF Toolkit

Merge, split and compress PDFs entirely in the browser.

- No upload
- No signup
- No watermark
- Ads only between actions (placeholders until AdSense is live)

## Tools

- `/` merge PDFs
- `/split` extract pages
- `/compress` reduce file size
